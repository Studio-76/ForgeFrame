[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "forgegate-backend"
version = "0.1.0"
description = "Initial backend scaffold for ForgeGate"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "."}
