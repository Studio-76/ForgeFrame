/**
 * ApiKeyRotateForm — rotation and status controls for a runtime key.
 *
 * Included as a sub-component of ApiKeyDetailPanel for separation of concerns.
 * Exported separately for potential standalone use.
 *
 * @packageDocumentation
 */

/**
 * This component is rendered inline inside {@link ApiKeyDetailPanel}.
 * It is extracted to its own file for module boundary clarity.
 *
 * The rotation button, status lifecycle buttons (activate/disable/revoke),
 * and request-path policy editor are all part of the detail panel.
 * See {@link ApiKeyDetailPanel} for the complete implementation.
 */
export {};
